/*import java.util.Scanner;
public class HelloJava {

    //fields/ properties

    //methods/ behaviors

    //your application will start from the "entrypoint" which defaults to the main method


    public static void main(String[] args) {
        // We have to begin with a hello world, its a necessity not a choice
        //public: access modifier
        //static: modifier
        //void: return type
        //(): arguments
        System.out.println("Hello Java!");
        IO.println("Hello Java!");

        //numerical types
        // declare a variable by type and name.
        // initialize a variable nu assigning a value
        int myint = 34; //Integer values
        double myDouble; //decimal value
        myDouble = 34.5;

        myint = 35;

        // Operators(mathematical : -, +, *, /, %)
        myint = 34 - 35;


        // Logical / comparison operators: >, <, ==, >=, <=, !=
        boolean myBool = myDouble > myint;
        // boolean can store true, false

        myBool = true;
        myBool = false;


        float; //floating point decimal(much more movement than double)
        long; //BIG integer values
        short; // small integer values
        byte; // REALLY small integer values

        // non-numerical types
        char; //character (letters, numbers, punctuation, etc)
        void; //seen mostly as return types, but generally empty
        boolean; //true or false values
        String; // an array of characters, ie words, sentences, etc
        null; // its not a zero, its an absence of value



        // control flow covers all of the keywords and functionality that allow an application
        // to make a decision and act on it without us providing additional input

        // if, else-if, else
        // switch, case
        // for, while, do while
        // "exception handling": try, catch


        //pseudo-code - describe the logic of your plan, don't worry about the exact syntax(rough draft)

        // initialize a boolean
        myBool = false;

        // make a choice based on the boolean
        if(myBool != true){
            IO.println("myBool was true");
        }
        else{
            IO.println("myBool was false");
        }

        String newString;
        newString = "Hello Everyone";

        IO.println(newString);
        // for(iterator, expression, increment)
        for (int i = 0; i < 13; i++ ) {
            System.out.println(newString.charAt(i));
        }

        // if true, do A
        // if false, do B


      //Array - a contiguous ordered collection of similar data type values
    // cant be modified once created
    // have to create a new one to modify




    }


}
*/